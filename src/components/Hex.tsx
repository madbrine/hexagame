import styled from "@emotion/styled";

function Hex() {
    
    const This = styled.button`
        padding: 32px;
        background-color: hotpink;
        font-size: 24px;
        border-radius: 4px;
        color: black;
        font-weight: bold;
        &:hover {
            color: white;
        }
    `
    return (
        <This>
            asd
        </This>
    );
}

export default Hex;